name: Release Drafter

on:
  push:
    branches:
      - "main"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  draft-release:
    uses: commit-check/.github/.github/workflows/release-drafter.yml@main
